using UnityEngine;

/// <summary>
/// Attached to invisible BeaconFactory object.
/// Opens and maintains connections to databases.
/// Takes requests from BM, gets data from DBs.
/// Translates data from server language to Unity format.
/// </summary>
public class DBConnector : MonoBehaviour {

}